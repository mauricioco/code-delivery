// @flow 
import { Grid } from '@material-ui/core';
import * as React from 'react';
type Props = {
  
};
export const Mapping = (props: Props) => {
  return (
    <div>
      <Grid item>Formulário</Grid>
      <Grid item>Map</Grid>
    </div>
  );
};